name = input("What is your name?")
print(name)

name = "Jack"
print(name)

name = "youn"
print(name)

print( len( input("What is your name?")))

name = input("What is your name?") # 데이터는 name에 저장되고 이 데이터를
Length = len(name) #Len 함수에서 사용하여 이 결과를 Length라는 다른 변수로 저장한다
print(Length) # 그리고 그변수로 출력한다.

# 변수 이름 짓기
N = "a"
N1 = "a"
#1N = "a" 숫자가 뒤에 가는건 상관 없지만 앞에가면 안된다
#N 1 = "a" 변수 이름 지을때 띄어쓰기는 안된다
N_1 = "A" # 하지만 _ 이어서 붙여준다
# print, input 같은 어떤 특정한 권한을 갖는 문자는 안된다
