class User:
    pass

user_1 = User()

# 파이썬에서 생성자를 만들려면 init 함수라는 특별한 함수를 사용해야 한다.
# __init__(): 밑줄두개 : 보통 속성을 초기화하는데 사용된다.
# class Car:
#     def __init__(self):
 # init 함수 안에서 속성을 초기화하거나 시작 값을 지정한다.
# 이 Class에서 새로운 객체를 만들 때마다
